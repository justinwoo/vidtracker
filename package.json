{
  "name": "vidtracker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "pp2n test",
    "build:get-icons": "pp2n bundle -m GetIcons -o get-icons.js",
    "build:be": "pp2n bundle -m Main -o index.js",
    "build:fe": "make build-fe",
    "build:generate-css": "pp2n bundle -m GenerateStylesheet -o generate-css.js && node generate-css.js",
    "build": "pp2n build && npm run build:fe && npm run build:be && npm run build:get-icons && npm run build:generate-css"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.4",
    "node-fetch": "^2.3.0",
    "prettier": "^1.16.1",
    "puppeteer": "^1.11.0",
    "sqlite3": "^4.0.6"
  },
  "devDependencies": {
    "parcel-bundler": "^1.11.0"
  },
  "engines": {
    "node": ">=8.0.0",
    "npm": ">=3.0.0"
  },
  "alias": {
    "echarts": "./src/echarts.js"
  }
}
